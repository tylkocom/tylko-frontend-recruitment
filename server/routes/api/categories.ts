
export default defineEventHandler(async (event): Promise<string[]> => {

    return [
        'wardrobe',
        'wallstorage',
        'tvstand',
        'bookcase',
        'sideboard',
    ]

});
